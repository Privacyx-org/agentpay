{
  "name": "agentpay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:marketplace": "pnpm -w --filter railent-sdk build && pnpm -w --filter marketplace build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express-rate-limit": "^7.5.1"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "@vercel/node": "^5.6.9"
  }
}
